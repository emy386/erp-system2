{
  "buildCommand": "pnpm --filter ./artifacts/kidzy run build",
  "outputDirectory": "artifacts/kidzy/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
